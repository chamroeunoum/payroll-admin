/vendor/
node_modules/
npm-debug.log
yarn-error.log

# Laravel 4 specific
bootstrap/compiled.php
app/storage/

# Laravel 5 & Lumen specific
public/storage
public/hot

# Laravel 5 & Lumen specific with changed public path
public_html/storage
public_html/hot
.DS_Store
storage/*.key
.env
# Vite layers .env.local over .env for BOTH dev and `vite build`. If it is
# present at build time it ships a bundle pointing at the local API, so it must
# never be committed. See .env for the commented local option.
.env.local
dist

# e2e test artifacts
cypress/screenshots/
cypress/videos/
test-results/
playwright-report/
Homestead.yaml
Homestead.json
/.vagrant
.phpunit.result.cache
